name: Strict PHP Test

on: [push]

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run Test Script
        # ここが重要：phpコマンドの実行結果を厳密にチェックさせます
        run: |
          php test.php
